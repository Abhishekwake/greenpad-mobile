# Production build (`next build` / `next start`) — copy to `.env.production`
# or set the same variable in Vercel / Docker / CI (recommended for secrets).

NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
